server {
    listen 80;
    server_name localhost;

    root /usr/share/nginx/html;
    index index.html index.htm;

    location / {
        try_files $uri /index.html;
    }

    gzip on;
    gzip_types text/javascript text/css text/plain;
    gzip_min_length 256;
}